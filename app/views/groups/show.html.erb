<h1><%= @group.name %></h1>

<p><%= @group.description %></p>

<nav>
  <ul>
    <li><%= link_to "Join", join_group_path(@group), method: :post %>
  </ul>
</nav>

<ul>
  <% @group.users.each do |user| %>
    <li><%= link_to user.email, user %></li>
  <% end %>
</ul>
